define(['skeleton'], function(sk) {
    var Model = sk.Model.extend({
        name: 'VcbEntry',
        idAttribute: 'id',
        configure: function(){

        }
    });
    return Model;
});