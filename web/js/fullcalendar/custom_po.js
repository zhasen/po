define(['jQuery', 'fullcalendar'], function (jQuery, fullcalender) {
    (function ($, fullcalender) {

        $('#calendar').fullCalendar({
            // put your options and callbacks here
        })

    })(jQuery);
    return jQuery;
});
