<%- layout('layout') %>
<a href="/news-admin-add" class="btn btn-warning" style="margin-top: 10px;margin-left:50px;text-align: center;">发布消息</a>
<table class="table table-hover" style="margin-top: 10px;margin-left:50px;text-align: center;">

    <thead>

    <tr>
        <th>发布者</th>
        <th>标题</th>
        <th>发布时间</th>
        <th>使用者</th>
        <th>操作</th>
    </tr>
    </thead>
    <tbody>
    <%
        for(var i=0;i<list.length;i++) {
            var item = list[i];
    %>
    <tr>
        <td>admin</td>
        <td><%=item.title%></td>
        <td><%=item.crtOn%></td>
        <td><%if(item.to == 1) {%> 学生 <% } else {%> 老师 <% } %></td>
        <td><a data-bypass href="/news-detail-<%=item.id%>">查看详细</a>&nbsp;&nbsp;|&nbsp;&nbsp;<a data-bypass href="/news-del-<%=item.id%>" onclick="return confirm('你确定要删除吗？')">删除</a> </td>
    </tr>
    <%
        }
    %>
    </tbody>
</table>
